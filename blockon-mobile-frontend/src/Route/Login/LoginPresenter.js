import React from "react";
import { Text } from "react-native";
import styled from "styled-components";

const LoginPresenter = () => {
  return <Text>LoginPresenter</Text>;
};

export default LoginPresenter;
